// define one variable
var greet : string = 'Hello';

// define another with implicit type:
var who = 'World';

console.log(greet + ' ' + who);

// string
var phrase : string = 'This is Typescript';

// number
var version : number = 5;
var weight : number = 78.6;

// boolean
var amIEvil : boolean = false;

// array
var names : string[] = ['Mobile','Console','Web'];

// any
var unknown : any;

console.log(greet + ' this is version ' + version);
console.log('Welcome to the ' + names[2]);
